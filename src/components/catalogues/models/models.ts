
export class ExpenseTypeModel {
    
    description: string;
    active: boolean;

    constructor(description: string, active: boolean) {
        this.description = description;
        this.active = active;
    }
}

export class IncomeTypeModel {
   
    description: string;
    active: boolean;

    constructor(description: string, active: boolean) {
        this.description = description;
        this.active = active;
    }
}

